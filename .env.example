# Keep the real value out of GitHub and out of source control
GITHUB_TOKEN=github_pat_replace_me
# Optional overrides; config.json is used by default
# GITHUB_REPO=owner/repository
# GITHUB_BRANCH=main
